import pandas as pd
df = pd.Series([1,2,3])          # it will create a single column with index starting with 0
print(df)                        # df is a variable name which we can use later to perform some actions like print it 